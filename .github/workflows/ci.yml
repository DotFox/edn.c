name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  test-linux:
    name: Test on Linux (${{ matrix.compiler }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [gcc, clang]

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Build library (${{ matrix.compiler }})
      run: |
        make CC=${{ matrix.compiler }}

    - name: Run tests
      run: |
        make test CC=${{ matrix.compiler }}

    - name: Run tests with enabled features
      run: |
        make clean
        make test CC=${{ matrix.compiler }} ALL=1

    - name: Run tests with sanitizers
      env:
        ASAN_OPTIONS: detect_leaks=1
      run: |
        make clean
        make DEBUG=1 test CC=${{ matrix.compiler }}

  test-macos:
    name: Test on macOS
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Install LLVM via Homebrew
      run: |
        brew install llvm
        echo "$(brew --prefix llvm)/bin" >> $GITHUB_PATH

    - name: Show architecture and compiler
      run: |
        uname -m
        clang --version

    - name: Build library
      run: make

    - name: Run tests
      run: make test

    - name: Run tests with enabled features
      run: |
        make clean test ALL=1

    - name: Run tests with sanitizers
      run: |
        make clean
        make DEBUG=1 CC=clang ALL=1 LDFLAGS="-L$(brew --prefix llvm)/lib" test

  test-windows:
    name: Test on Windows (MinGW)
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Setup MSYS2
      uses: msys2/setup-msys2@e9898307ac31d1a803454791be09ab9973336e1c # v2.31.1
      with:
        msystem: MINGW64
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-make
          make

    - name: Build library
      run: make

    - name: Run tests
      run: make test

    - name: Run tests with enabled features
      run: |
        make clean
        make test ALL=1

  test-windows-msvc:
    name: Test on Windows (MSVC + CMake)
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

    - name: Configure CMake
      run: cmake -B build -DCMAKE_BUILD_TYPE=Release

    - name: Build
      run: cmake --build build --config Release

    - name: Run tests
      run: ctest --test-dir build -C Release --output-on-failure

    - name: Configure CMake with Debug mode and sanitizers
      run: cmake -B build-debug -DEDN_ENABLE_DEBUG=ON

    - name: Build with Debug mode and sanitizers
      run: cmake --build build-debug --config Debug

    - name: Run tests with sanitizers
      run: ctest --test-dir build-debug -C Debug --output-on-failure

  benchmark-linux:
    name: Benchmark on Linux
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Prepare java
      uses: actions/setup-java@17f84c3641ba7b8f6deff6309fc4c864478f5d62 # v3.14.1
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Install Clojure CLI
      uses: DeLaGuardo/setup-clojure@4c7a6f613e5089821bb3bb2a33a3ee115578580d # 13.6.1
      with:
        cli: latest

    - name: Build and run benchmarks
      run: make bench-compare

  benchmark-macos:
    name: Benchmark on macOS
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Prepare java
      uses: actions/setup-java@17f84c3641ba7b8f6deff6309fc4c864478f5d62 # v3.14.1
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Install Clojure CLI
      uses: DeLaGuardo/setup-clojure@4c7a6f613e5089821bb3bb2a33a3ee115578580d # 13.6.1
      with:
        cli: latest

    - name: Build and run benchmarks
      run: make bench-compare

  benchmark-windows:
    name: Benchmark on Windows (MinGW)
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Setup MSYS2
      uses: msys2/setup-msys2@e9898307ac31d1a803454791be09ab9973336e1c # v2.31.1
      with:
        msystem: MINGW64
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-make
          make

    - name: Prepare java
      uses: actions/setup-java@17f84c3641ba7b8f6deff6309fc4c864478f5d62 # v3.14.1
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Install Clojure CLI
      uses: DeLaGuardo/setup-clojure@4c7a6f613e5089821bb3bb2a33a3ee115578580d # 13.6.1
      with:
        cli: latest

    - name: Build and run benchmarks
      shell: pwsh
      run: |
        make bench
        clojure -M -m bench-integration

  benchmark-windows-msvc:
    name: Benchmark on Windows (MSVC)
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Prepare java
      uses: actions/setup-java@17f84c3641ba7b8f6deff6309fc4c864478f5d62 # v3.14.1
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Install Clojure CLI
      uses: DeLaGuardo/setup-clojure@4c7a6f613e5089821bb3bb2a33a3ee115578580d # 13.6.1
      with:
        cli: latest

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

    - name: Configure and build
      run: |
        cmake -B build -DCMAKE_BUILD_TYPE=Release
        cmake --build build --config Release

    - name: Run benchmarks
      run: |
        ./build/Release/bench_integration.exe
        clojure -M -m bench-integration

  # Code formatting check
  format-check:
    name: Check code formatting
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

    - name: Install clang-format
      run: |
        sudo apt-get update
        sudo apt-get install -y clang-format

    - name: Check formatting
      run: |
        make format-check
